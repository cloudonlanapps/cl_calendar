name: cl_calendar
description: A Flutter calendar widget with month, week, and day views. Built with Riverpod and shadcn_ui.
version: 0.0.1

environment:
  sdk: ^3.11.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.0
  shadcn_ui: ^0.54.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.0.0
